on:
  workflow_dispatch:
  push:
    branches: ['main']

jobs:
  hello:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
    - uses: octo-sts/action@v1.0.0
      id: octo-sts
      with:
        scope: imjasonh/workflow-private
        identity: workflow-public
    - env:
        GITHUB_TOKEN: ${{ steps.octo-sts.outputs.token }}
      run: |
        set -x 
        
        now=$(printf `date +"%Y-%m-%dT%H:%M:%S%:z"`)
        echo "now: $now"

        gh workflow run --repo imjasonh/workflow-private private.yaml --ref main

        sleep 5        
        num=$(gh run list --repo imjasonh/workflow-private -w private.yaml -e workflow_dispatch --created ">=${now}" --json number | jq -r '.[0].workflowDatabaseId')
        if [ "$num" = "null" ]; then
          echo "No run found"
          exit 1
        fi
        echo "num: $num"
        echo "Workflow link: https://github.com/imjasonh/workflow-private/actions/runs/${num}"
        while true; do
          status=$(gh run list --repo imjasonh/workflow-private -w private.yaml -e workflow_dispatch --created ">=${now}" --json status | jq -r '.[0].status')
          echo "status: $status"
          if [ "$status" = "null" ]; then
            echo "Status not found"
            exit 1
          fi
          if [ "$status" = "completed" ]; then
            break
          fi
          sleep 5
        done
